# ----------------------------------------
# Игнорировать скомпилированные файлы и исполняемые файлы
# ----------------------------------------

# Игнорировать исполняемый файл (может быть "code" или "code.exe")
code
code.exe

# Игнорировать все объектные файлы (.o), если они генерируются в корне
*.o

# Игнорировать все динамические библиотеки (.so) в любой папке
*.so

*.a

# ----------------------------------------
# Игнорировать каталог /libs целиком
# ----------------------------------------

# Игнорировать весь каталог libs, независимо от его содержимого
# (Это гарантирует, что даже исходники .c в этой папке не попадут в репозиторий)
*/libs/*.so

# ----------------------------------------
# Игнорировать все текстовые файлы (.txt)
# ----------------------------------------

# Игнорировать все файлы, заканчивающиеся на .txt
*.txt

# ----------------------------------------
# Дополнительные файлы, которые не нужно хранить
# ----------------------------------------
Thumbs.db