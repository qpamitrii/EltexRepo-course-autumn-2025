# ----------------------------------------
# Игнорировать исполняемые файлы
# ----------------------------------------

# Исполняемый файл калькулятора (может называться 'code' или 'a.out')
code
code.exe
a.out

# ----------------------------------------
# Игнорировать скомпилированные библиотеки и объектные файлы
# ----------------------------------------

# Игнорировать все динамические библиотеки (.so) в папке libs
# Это самый важный шаблон для вашего проекта, так как библиотеки генерируются.
libs/*.so

# Если вы используете объектные файлы (.o), игнорируем их везде
*.o
# ----------------------------------------
# Вспомогательные файлы IDE/ОС (полезно для любого проекта)
# ----------------------------------------
# Игнорировать файлы, которые создает VS Code, Sublime или другие редакторы
.vscode/
*.swp
*~
# Игнорировать файлы отладчика
core

# Игнорировать вспомогательные файлы Windows
Thumbs.db

*.txt